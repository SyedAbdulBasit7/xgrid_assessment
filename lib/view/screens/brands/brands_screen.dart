import 'package:flutter/material.dart';

class BrandScreen extends StatelessWidget {
  static const routeName = '/brand-screen';
  const BrandScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Placeholder(),
    );
  }
}
